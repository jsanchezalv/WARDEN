# Creates a discrete resource from an external pointer

Creates a discrete resource from an external pointer

## Usage

``` r
.resource_discrete_from_xptr(xptr)
```

## Arguments

- xptr:

  external pointer from resource_discrete()\$.ptr

## Value

new cloned discrete resource
