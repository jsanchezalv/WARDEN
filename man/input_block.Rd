% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_f.R
\name{input_block}
\alias{input_block}
\title{Build an input block for automatic parameter selection}
\usage{
input_block(
  .data = NULL,
  base,
  psa,
  sens,
  names_out,
  psa_indicators = NULL,
  sens_indicators = NULL,
  indicator_sens_binary = FALSE,
  dsa_names = NULL,
  distributions = NULL,
  covariances = NULL
)
}
\arguments{
\item{.data}{Optional existing \code{{}} block to prepend (for pipe chaining).}

\item{base}{A list of base case values, one entry per parameter.}

\item{psa}{An unevaluated expression producing PSA draws (e.g.
\code{pick_psa(...)}), evaluated at runtime per simulation.}

\item{sens}{The sensitivity data object (e.g. \code{l_inputs}). At runtime the
engine indexes it as \code{sens[[sens_name_used]]} to pick the active column.}

\item{names_out}{Character vector or list of strings of output parameter names.}

\item{psa_indicators}{List of 0/1 indicators controlling which parameters
draw from PSA. \code{NULL} means all parameters draw from PSA.}

\item{sens_indicators}{List of indicators, one entry per parameter (may be
a scalar or a vector for vector-valued parameters). In binary mode: 0 =
inactive, 1 = active. In grouped mode: 0 = inactive, same integer =
varied together. \code{NULL} means all parameters are active. Only in grouped
mode can subparameters (e.g., elements of a vector parameter) be varied
at different steps of a DSA.}

\item{indicator_sens_binary}{Logical. \code{TRUE} = binary (0/1) mode; each
active parameter is varied independently via \code{\link[=create_indicators]{create_indicators()}}.
\code{FALSE} (default) = grouped integer mode when \code{sens_indicators} and
\code{distributions} are supplied; otherwise falls back to binary mode.}

\item{dsa_names}{Character vector of \code{sensitivity_names} values that are
DSA directions (e.g. \code{c("DSA_min","DSA_max")}). Those names iterate
through active parameters/groups one at a time. All other names passed
to \code{sensitivty_names} in \code{\link[=run_sim]{run_sim()}} are treated as scenarios. \code{NULL}
(the default) means all sensitivity names are scenarios.}

\item{distributions}{List of distribution names (e.g. \code{"rnorm"},
\code{"mvrnorm"}, \code{"rdirichlet"}), required in grouped mode to determine
which parameters are vectors and require conditional distributions.}

\item{covariances}{List of covariance matrices or scalars, required for
\code{"mvrnorm"} / \code{"rdirichlet"} distributions.}
}
\value{
A \code{{}} language object (same type as \code{\link[=add_item]{add_item()}}) with a
\code{warden_block_meta} attribute used by the engine to build the iteration
schedule.
}
\description{
Creates an unevaluated \code{{}} expression that calls \code{pick_val_v()} with the
correct arguments for base case, PSA, DSA, and scenario analyses.
The expression is meant to be passed directly to \code{run_sim()} or
\code{run_sim_parallel()} as a \verb{*_inputs} argument.
}
\details{
Supports two modes:
\itemize{
\item \strong{Binary} (\code{indicator_sens_binary = TRUE}): \code{sens_indicators} are 0/1
per parameter. Each non-zero parameter is varied independently via
\code{\link[=create_indicators]{create_indicators()}}.
\item \strong{Grouped} (\code{indicator_sens_binary = FALSE}, the default when
\code{sens_indicators} and \code{distributions} are supplied): \code{sens_indicators}
are integer group labels. Parameters sharing the same integer are varied
together; 0 means the parameter is never varied.
}
\subsection{DSA vs scenario analyses}{

Supply \code{dsa_names} with the subset of \code{sensitivity_names} that correspond
to DSA directions (e.g. \code{c("DSA_min","DSA_max")}). Each DSA name runs one
iteration per active parameter/group; other names (scenarios) apply all
active parameters simultaneously in a single iteration.

When \code{dsa_names = NULL} (the default) every sensitivity name is treated as a
scenario -- all active parameters take their scenario value at once, one
iteration per \code{sensitivity_names} entry. This is the correct default when
there is no DSA.
}

\subsection{\code{sens_indicators} and zeros}{

Any parameter whose \code{sens_indicators} entry is \code{0} (or all-zero for
vector-valued parameters) is \strong{never varied}: it always uses the base or
PSA value regardless of whether we are running a DSA or a scenario. This
applies to both modes.
}
}
\examples{
l_inputs <- list(
  parameter_name = list("util.sick", "util.sicker"),
  base_value     = list(0.8, 0.5),
  PSA_dist       = list("rnorm", "rbeta_mse"),
  a              = list(0.8, 0.5),
  b              = list(0.04, 0.025),
  n              = list(1, 1),
  DSA_min        = list(0.6, 0.3),
  DSA_max        = list(0.9, 0.7),
  psa_indicators = list(1, 1)
)

blk <- input_block(
  base                  = l_inputs[["base_value"]],
  psa                   = pick_psa(l_inputs[["PSA_dist"]], l_inputs[["n"]],
                                   l_inputs[["a"]], l_inputs[["b"]]),
  sens                  = l_inputs,
  names_out             = l_inputs[["parameter_name"]],
  psa_indicators        = l_inputs[["psa_indicators"]],
  indicator_sens_binary = TRUE,
  dsa_names             = c("DSA_min", "DSA_max")
)
stopifnot(is.call(blk))
stopifnot(!is.null(attr(blk, "warden_block_meta")))
}
\seealso{
\code{\link[=pick_val_v]{pick_val_v()}}, \code{\link[=add_item]{add_item()}}, \code{\link[=run_sim]{run_sim()}}
}
