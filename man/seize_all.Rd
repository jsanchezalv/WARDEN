% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_f.R
\name{seize_all}
\alias{seize_all}
\title{Seize multiple discrete resources atomically}
\usage{
seize_all(resources, policy = "all_or_none", amounts = NULL, priorities = NULL)
}
\arguments{
\item{resources}{A list of \code{resource_discrete} objects.}

\item{policy}{\code{"all_or_none"} (default) — acquires all only if all are
available; queues for the first bottleneck without holding any others.
\code{"sequential"} — acquires in list order, holding partial acquisitions;
user is responsible for avoiding deadlocks.}

\item{amounts}{Integer vector of units per resource (default \code{1L} for each).}

\item{priorities}{Integer vector of priorities per resource (default \code{1L}).}
}
\value{
\code{TRUE} if all acquired, \code{FALSE} if queued for a bottleneck resource,
\code{NA} if rejected.
}
\description{
Attempts to acquire a list of resources in a single C++ call, avoiding
per-resource R-to-C++ round trips.
}
